<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_soft_gradient">

    <TextView
        android:id="@+id/tvStatus"
        android:text="X's Turn"
        android:textStyle="bold"
        android:textSize="24sp"
        android:textColor="@color/black"
        android:layout_marginBottom="24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- ORIGINAL GRID (NO ANDROIDX) -->
    <GridLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="3"
        android:alignmentMode="alignMargins"
        android:useDefaultMargins="true">

        <Button android:id="@+id/btn00" style="@style/TicButton"/>
        <Button android:id="@+id/btn01" style="@style/TicButton"/>
        <Button android:id="@+id/btn02" style="@style/TicButton"/>

        <Button android:id="@+id/btn10" style="@style/TicButton"/>
        <Button android:id="@+id/btn11" style="@style/TicButton"/>
        <Button android:id="@+id/btn12" style="@style/TicButton"/>

        <Button android:id="@+id/btn20" style="@style/TicButton"/>
        <Button android:id="@+id/btn21" style="@style/TicButton"/>
        <Button android:id="@+id/btn22" style="@style/TicButton"/>

    </GridLayout>

    <Button
        android:id="@+id/btnReset"
        android:text="NEW GAME"
        android:textColor="@color/white"
        android:backgroundTint="@color/green"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>